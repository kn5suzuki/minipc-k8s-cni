# Generated by `make seeds`/`make vms` — cloud image + per-node seed ISOs
images/*.img
images/*.iso

# Claude Code local permission overrides (machine/session-specific)
.claude/settings.local.json

# OS / editor cruft
.DS_Store
*.swp
